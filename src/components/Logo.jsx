import React from 'react'

const Logo = ({name}) => {
  return (
    <div style={{color:"whitesmoke",height:"25px",marginTop:"-8px",cursor:"pointer"}} >
        <h3>{name}</h3>
    </div>
  )
}

export default Logo